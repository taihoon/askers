import { DistanceToNowPipe } from './distance-to-now.pipe';

describe('DistanceToNowPipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceToNowPipe();
    expect(pipe).toBeTruthy();
  });
});
